#include<stdio.h>
void factorial(int x,int n){
	
	if(x>n)
	return;
	printf("%d",x);
	factorial(x+1,n); 
	return ;
}
main()
{
	int n;
	printf("Enter the number \n");
	scanf("%d",&n);
	factorial(1,n);
}
