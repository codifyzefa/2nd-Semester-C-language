#include<stdio.h>
main()
{
	// sum of given number 
	int num,sum=0;
	printf("enter the value : \n");
	scanf("%d",&num);
	while(num!=0){
		int last_digit=0;
		last_digit=num%10;
		sum=sum+last_digit;
		
	}
	printf("%d",sum);

}
