#include<stdio.h>
main()
{
	char temp;
	char arr[40];
	gets(arr);
	puts("The reverse arrays is : ");
	
	int i,size=0;
	while(arr[i]!='\0'){
		size++;
		i++;
}
	for(int i=0,j=size-1;i<=j;i++,j--){
		temp=arr[i];
		arr[i]=arr[j];
		arr[j]=temp;
	}

	puts(arr);
}
